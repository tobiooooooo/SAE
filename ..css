/* Global Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background-color: #f5f5f5;
    color: #333;
}

/* Header Styles */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    background-color: #e6f3ff;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

header .logo img {
    width: 100px;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
}

nav ul li a {
    text-decoration: none;
    color: #333;
    font-weight: bold;
}

.auth-buttons button {
    margin-left: 10px;
    padding: 8px 16px;
    background-color: #333;
    color: #fff;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

.auth-buttons button:hover {
    background-color: #555;
}

/* Hero Section */
.hero {
    text-align: center;
    padding: 50px;
    background: linear-gradient(to bottom, #cce7ff, #b3daff);
}

.hero h1 {
    font-size: 48px;
    color: #333;
}

.cta {
    padding: 10px 20px;
    margin: 20px 0;
    background-color: #333;
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

.cta:hover {
    background-color: #555;
}

.side-info {
    text-align: right;
    padding: 20px;
    font-style: italic;
    color: #666;
}

/* Articles Section */
.articles {
    padding: 40px;
    background-color: #e6f3ff;
}

.article-row {
    display: flex;
    justify-content: space-around;
    margin-bottom: 20px;
}

.article-card {
    background-color: #ffffff;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    padding: 20px;
    width: 300px;
    text-align: center;
    transition: transform 0.3s;
}

.article-card:hover {
    transform: translateY(-5px);
}

.article-card img {
    width: 100%;
    height: 150px;
    object-fit: cover;
    border-radius: 5px;
}

.article-card h2 {
    font-size: 24px;
    margin-top: 10px;
    color: #333;
}

.article-card p {
    font-size: 14px;
    color: #666;
    margin: 10px 0;
}

.article-card button {
    padding: 10px;
    background-color: #333;
    color: #fff;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    margin-top: 10px;
}

.article-card button:hover {
    background-color: #555;
}

/* Footer Styles */
footer {
    padding: 40px;
    background-color: #cce7ff;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.footer-info p {
    margin: 5px 0;
}

.footer-links a {
    margin-left: 15px;
    text-decoration: none;
    color: #333;
    font-weight: bold;
}

.footer-links a:hover {
    color: #555;
}

.support-btn {
    background-color: #333;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

.support-btn:hover {
    background-color: #555;
}
